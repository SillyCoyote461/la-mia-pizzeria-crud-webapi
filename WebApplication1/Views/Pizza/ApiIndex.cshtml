<main class="pt-4">
    @if (User.IsInRole("ADMIN"))
    {
        <div class="mb-2">
            <a class="btn btn-success" href="@Url.Action("ApiCreate", "Pizza")"> Add pizza</a>
        </div>
    }

    <div class="input-group input-group-sm mb-3">
        <span class="input-group-text" id="inputGroup-sizing-sm">Filtra pizze</span>
        <input id="filter-bar" oninput="filtered()" type="text" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-sm">
    </div>
    <div>
        @*pizza container*@
        <div id="table" class="d-flex justify-content-evenly flex-wrap">


        </div>
    </div>
</main>

@section Scripts{
    <script>
        initIndex("");
    </script>
}